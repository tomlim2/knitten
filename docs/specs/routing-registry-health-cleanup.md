# Routing Registry Health Cleanup

Status: compatibility pointer.

Knitten no longer uses this as the canonical name for the output registry
contract. Use
[`output-registry-health-cleanup.md`](output-registry-health-cleanup.md) for the
current spec.
