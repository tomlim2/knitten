# Log: vrm2u retarget MetaHuman

| Time | Agent | Entry |
|------|-------|-------|
| 2026-03-30 | #1 | Ops initialized. cinev_retarget 탐색 완료 — 이미 MetaHuman→VRM 54본 매핑 존재. 테스트 환경 구축 중심 플랜. |
| 2026-03-30 | #1 | R-001 done. FBX 4개(anim 2+model 2)→megamelange push. R-002 done. cinev_male_body.json 생성(female 기반, name만 변경). |
| 2026-03-30 | #3 | R-003 done. cinev_retarget integration tests 15개 추가 (tests/integration.rs). config 파싱 3, FBX 파싱 2, body retarget 2(v0/v1), bone count 1, skeleton 2, male retarget 1, facial-only FBX 1, invalid FBX 1, invalid config 1. 발견: flutter FBX는 facial-only (bone tracks 1개), body tests는 rush FBX 사용. 15/15 통과. |
| 2026-03-30 | #3 | R-004 done. T2M Retrieve API(10.77.20.63:24000)로 FBX 3개 생성. Generate API(10.77.28.200:20000) 접근 불가. t2m_m_walk.fbx(3.6MB), t2m_m_wave.fbx(4.1MB), t2m_f_walk.fbx(4.0MB). 모두 FBX binary magic 확인, fbx-dump 파싱 성공. Blender 4.5.4 LTS 출력. |
| 2026-03-30 | #2 | R-005 done (partial). RQ 실행 불가 — Retargeter는 VrmRestPose(Bevy 런타임 VRM 로드) 필요, headless CLI 없음. 대신: (1) 15/15 integration tests pass (male config 포함), (2) male/female FBX skeleton 비교 — 동일 84-bone, pelvis ~동일(93.8/94.1cm), limb 비율 차이(female thigh 17% 길다, male arm 17% 길다), pre-rotation 전부 identity. rq-results.md에 문서화. |
| 2026-03-30 | #2 | R-007 done. bevy-vrm workspace clippy/fmt 정리. 수정 21건: derivable_impls 1, type_complexity 10(allow), too_many_arguments 3(allow), collapsible_if 4(collapse), is_multiple_of 3, dead_code 1(allow), unused_assignment 1(remove). cargo fmt/clippy/test 3종 통과. 로직 변경 없음. |
| 2026-03-30 | #3 | R-006 done. 5개 조합 RQ diagnostics (rq_diagnostics.rs 5 tests, 5/5 pass). 전체 53 bone tracks, 51/51 matched, 0 unmatched. KI-1(arm asymmetry): 재현 — wave rUA 118.2° vs lUA 68.9° (49.2° 차이, 소스 모션 자체가 비대칭). KI-2(wrist flex): 부분 재현 — rH raw 100.1° (damping 전). KI-3(VRM 0.x 180°Y): 미재현 — mapping 레벨에서 v0/v1 동일 (Retargeter 레벨 검증 필요). rq-issues.md에 문서화. |
| 2026-03-30 | #3 | R-008 done. headless retarget CLI 구현. 신규 파일: vrm_rest.rs (GLB→VrmRestPose 추출, gltf crate), bin/headless.rs (CLI). Bevy 없이 VRM rest pose 추출 + Retargeter 실행 + RQ 출력. xiao_vroid.vrm + t2m_m_walk.fbx 검증: bones=53 scale=1.018 shoulder=0.58 arm=0.83 identity=PASS 52/52. clippy clean (내 파일), tests 20/20 pass. |
| 2026-03-30 | #3 | R-009 done. 8개 조합 headless CLI 테스트. 6/8 성공, 1 에러(0.x missing ext), 1 ABORT(facial FBX stack overflow). **핵심: Bevy viewer RQ와 headless CLI 동일 (scale=1.017, shoulder=0.68, arm=0.94).** Male FBX는 VRM보다 넓고 긴 팔다리(shoulder 0.58, arm 0.83). VRM 0.x는 사전 변환 필요(shimaenaga: scale 0.111 — 좌표계 미변환). zepeto: 소형 아바타(scale 0.363)도 identity PASS. headless-results.md에 문서화. |
| 2026-03-30 | #2 | R-011 done. fbxcel stack overflow 수정. **원인: fbxcel이 아니라 compute_fbx_skeleton_from_parsed()의 재귀 topo sort에서 parent cycle → 무한재귀.** facial FBX(114 bones, blend shape geometry 포함)에서 cycle 존재. 수정 3건: (1) 재귀→iterative topo sort + cycle detection, (2) headless.rs: double FBX parse 제거(retarget_with_skeleton() 사용), (3) cinev_retarget clippy 20 warnings 정리. 수정 후 facial FBX 정상 완료(1 bone, 6.7s). body FBX 회귀 없음. fmt/clippy/test 20/20 통과. fbxcel-analysis.md에 문서화. |
| 2026-03-30 | #3 | R-010 done. vrm0_compat을 cinev_retarget에 복사(옵션A). headless CLI에 자동 감지+변환 추가. 검증: xiao(1.0) scale=1.018 PASS 동일. **moth(0.x) 자동 변환 성공:** vrm=0.x(converted) scale=0.145 shoulder=1.68 arm=0.66 root180=Y identity PASS 33bones. shimaenaga는 이미 1.0(VRMC_vrm 존재) — scale=0.111은 모델 크기(~10cm 마스코트). 20/20 tests pass, clippy clean. |
| 2026-03-30 | #3 | R-012 done. T2M vs CINEV FBX 5개 파일 구조 비교 완료. arm-stats 분석 도구 작성. **핵심 결론: (A) T2M walk은 소스 자체가 arm range 10-40x 작음(dance vs walk). (B) shoulder_ratio=0.58일 때 blend=0.81로 19% 추가 감쇠. (C) PreRotation 차이(CINEV=있음, T2M=identity/Blender bake)는 retarget 수식상 정확 — 문제 아님.** t2m-vs-cinev-spec.md에 전체 문서화. |
| 2026-03-30 | #3 | R-013 done. shoulder slerp 5 variants 실험 (E-0~E-4). 3 FBX × 5 공식 = 15회 headless CLI 실행. **핵심: E-4 `max(0.90, current)` 추천.** walk arm range 5.7→6.4° (+12%, 미미), wave R arm 74.0→82.9° (+12%, 유의미), CINEV rush 106.7→112.2° (+5%, 회귀 없음). Walk 개선은 근본적으로 T2M 소스 한계 — slerp으로 해결 불가. shoulder-slerp-experiment.md에 문서화. |
| 2026-03-30 | #3 | R-015 done. 본 매핑 정합성 검증. 51 direct_map 전체 매칭 (unmatched_config=0). **twist bone 12개 전부 MISSING** — T2M/CINEV 모두 baked FBX라 twist bone 미포함. VRM 타겟에는 불필요 (VRM도 twist bone 없음). FBX 미매핑 29개는 의도적 (metacarpal=ignore, toe phalanges=VRM 미지원, neck_02=미사용). **Hand rest delta가 T2M에서 더 큼** (leftHand Z축 59° vs CINEV 9°) — three-VRM 수식이 처리하므로 문제없음. **결론: 매핑 완전 정확. arm 약함은 매핑 문제 아님.** bone-mapping-audit.md에 문서화. |
| 2026-03-31 | #3 | R-016 done. 리타게터 충실도 단일 수치 증명. `pre_damping_rotations` 필드 추가하여 damping 전/후 비교. **핵심: 리타게터는 소스에 공정.** T2M walk=0.985, T2M wave=0.977, CINEV rush=0.970 — CINEV가 오히려 가장 낮음(hand damping 때문). 22개 bone 중 20개 = 1.000 완벽 전달. damping 대상: upperArm(shoulder slerp 0.80~0.86), hand(60° soft damping, dance에서만 발동). **45° 회전 문제 해소:** three-VRM 정규화가 PreRotation 인코딩 차이(T2M identity vs CINEV separated) 올바르게 처리. hips frame 0 = 7.2° (45° 아님). rest-pose-fidelity.md에 문서화. |
| 2026-03-31 | #1 | R-024 done (reverted). detect_apose를 is_vrm0 게이트 → 효과 없음 (원인 아님). threshold 15°로 변경 → shoulder 살리고 upperArm 스킵했으나 근본 해결 안 됨. |
| 2026-03-31 | #1 | R-025 blocked. detect_apose 78° correction 방향 문제 집중 조사. post/pre/inv 4조합 + parent_global basis 전부 실패. FBX GlobalSettings 확인: FrontAxis=Y, FrontAxisSign=-1 (Blender). 180°Y coord_rot 시도 → 팔 OK but 다리 반전 (글로벌 변경 불가). RetargetedAnimation에 source_detected/resolved 추가. headless CLI에 arm+leg FK world pos 출력 추가. rest_rotation_euler fallback 수정 (track 없는 본). CLI 수치상 hand FORWARD 정상이나 viewer에서 방향 불일치 지속. |
| 2026-03-31 | #1 | R-026. detect_apose 10+가설 실험. 좌표계 일치 확인 (VRM1.0/FBX 모두 Y-up +Z-forward). three-vrm 수식 원본 검증 — pixiv/three-vrm과 수학적 동일, A-pose 미지원은 설계 의도 (T-pose 전제). **시도 요약:** (1) from_rotation_arc 다양한 basis change 5종 → 전부 ~5cm Z arcing error (2) XY projection/pure Z축 → 악화 (3) FBX local rest 직접 사용 → MetaHuman bone convention 불일치 (4) bone_rest_yup를 dst_rest_global로 교체 → 동일 불일치 (5) up-vector constraint + look_rotation → 미미한 개선 (6) FK inverse (parent world 역산) → twist 한계 동일 (7) src_rest injection → 좌우 비대칭 space mismatch (8) translation 보정 (arcing compensation) → CLI에서 Z NEUTRAL 달성했으나 **skinned mesh에서 bone position 이동 → mesh 분리 발생** (bone이 mesh와 분리됨). **근본 원인 확정:** rotation-only correction은 FK chain의 spine X-tilt(~23°)와 결합시 L×sin(78°)×sin(23°)≈7.5cm/segment의 arcing error 발생. translation 보정은 skinned mesh에서 불가. **시니어 자문:** Swing-Twist Decomposition, Up-Vector Constraint, Global-to-Local 역산, IK 보정 4가지 제안받음. **미해결:** rotation만으로 arcing error 없이 A-pose correction하는 방법. |
| 2026-03-30 | #2 | R-014 done. RQ 품질 경고 시스템 추가. 신규 타입: `RqGrade`(A/B/C/F), `RqDiagnostic`(warn/error). 6개 메트릭 체크: scale(0.8~1.3/0.5~2.0), shoulder(<0.7/<0.5), arm(<0.8/<0.6), identity(1~3/>3), bones(<45/<30), unmatched(1~3/>3). 각 경고에 원인 추정+해결 힌트 포함. headless CLI exit 1 on grade F. **검증 4조합:** xiao+rush=B(shoulder warn), xiao+t2m=B(shoulder warn), shimaenaga=F(3 errors: scale 0.111, shoulder 0.09, arm 0.15), zepeto=F(scale 0.364). clippy 0 warn, 5/5 tests pass. feat/rq-strict-warnings 브랜치. |
