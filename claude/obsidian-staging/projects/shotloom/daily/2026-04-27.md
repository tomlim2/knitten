---
title: "Shotloom daily — 2026-04-27"
tags:
  - shotloom
  - daily
  - retrospective
  - adr-discipline
date: 2026-04-27
source: claude
---

# Shotloom daily — 2026-04-27


## 15:39 — STL-208 closed (merged) — 회고: ADR이 코드 심볼 인용하면 어떻게 썩는가

**PR:** [#179](https://github.com/CINEV/shotloom/pull/179) feat(body-anim-normalizer): scaffold + canonical output type ownership — MERGED 2026-04-27T06:36Z
**Linear:** [STL-208](https://linear.app/cinamon-corp/issue/STL-208) In Review → Done (auto via GitHub integration)
**Umbrella:** [STL-194](https://linear.app/cinamon-corp/issue/STL-194) In Progress 유지 — 함수 이동 sub-issue 남음 ([STL-183](https://linear.app/cinamon-corp/issue/STL-183) 게이팅)
**Branch:** `feat/scaffold-body-anim-normalizer` (삭제됨 — squash 아님, 정상 fast-forward)
**Worktree:** `.worktrees/stl-194-body-anim-normalizer` (제거됨)
**Commits on branch:** 7 — `c0648f4` scaffold → `e339764` ADR-0025 sentinel rewrite

### 받은 지적 (ryumiel, P1 Blocking, 인라인 1건)

ADR-0025 §sentinel-convention 단락에 `mapping::retarget(...)` 라는 모듈 심볼이 괄호 예시로 박혀 있었음. 이번 PR 의 rename (`mapping::retarget` → `mapping::normalize`) 으로 그 심볼이 stale 됐는데, reviewer 가 짚은 핵심은 **rename 으로 stale 된 게 결함이 아니라, ADR 본문에 코드 심볼 자체를 박은 게 결함** 이라는 것. 수정 방향이 `normalize` 로 갱신이 아니라 도메인 용어로 다시 쓰기였던 이유.

> [!info] 근거
> `docs/guidelines/documentation-standard.md` §5.7 ("ADRs preserve durable why") + §5.4 ("describe intended behavior, not implementation detail")

### 무엇이 새로 보였는가

> [!tip] 패턴 보강
> 기존 G10 패턴 (`~/.claude/standards/review-code-rust.md`) 은 schema/data shape/Rust signature 같은 **큰 덩어리** 위주의 ADR-impl-detail 침범을 다루고 있었음. 이번 케이스는 정반대로 **한 줄짜리 심볼 인용** — 작아서 self-review 때 안 걸렸음. "ADR 안에 `module::function(...)` 한 줄도 들어가면 안 된다" 는 명제가 충분히 강하지 않았다는 뜻. G10 두 번째 Real defect 로 캡처해서 다음부터는 self-check 때 grep 패턴으로 잡힐 수 있게 함.

### 다음에 다르게 할 것

- ADR 자기검토 시 `rg '\`[a-z_]+::[a-z_]+' docs/adr/` 한 번 돌리기 — backtick 안에 `::` 있으면 거의 코드 경로
- "예시는 도메인 단어로" 를 ADR drafting 의 default 로. 모듈 경로 쓰고 싶어지면 → 같은 의미를 도메인 용어로 풀 수 있는지 먼저 시도
- §5.7 / §5.4 가 짧은 인용에도 적용된다는 점이 review-code-rust.md G10 에 명시됐으니 [[shotloom-review-before-pr]] 가 다음부터 잡아야 함

### 곁가지

> [!warning] 패턴 인헤리트
> ADR-0025 자체가 STL-194 PR 인데 다른 ADR (-0030) 의 STL-195 PR ([#178](https://github.com/CINEV/shotloom/pull/178)) 에서도 같은 결의 P1 두 건이 또 나옴 — "ADR 을 progress tracker 로 쓰지 말라" 같은 documentation-standard 위반이 한 시점에 여러 PR 에서 동시에 터짐. 패턴이 한 번 미끄러지면 형제 PR 에 인헤리트되는 경향이 있음을 인지.

- PR #179 본문이 "Part of STL-194" 로 적혀 있었지만 Linear 가 PR-issue 자동 링크로 STL-208 (실제 sub-issue) 를 잡아 Done 처리해줌 — 명시 linkage 와 Linear auto-detection 이 다를 수 있다는 것.

#rule #adr-discipline #review-pattern #g10
