---
title: "ADR-0047 Tailwind styling default — same-PR self-precedent sweep"
tags:
  - type/devlog
  - project/shotloom
  - area/adr
date: 2026-05-13
source: claude-code
---

# ADR-0047 Tailwind styling default — same-PR self-precedent sweep

## 14:06 — STL-397 closed ([#311](https://github.com/CINEV/shotloom/pull/311))

회고 — ADR-0047 (Tailwind as editor styling default) Proposed 등록. 리뷰 라운드 1에서 `rename-fragile prose` 동일 패턴이 같은 ADR 안에서 두 번 더 적발됨.

**지적 1 — Decision §2가 v3 형태로 token-storage shape을 pin하고, 별도 ADR(0017)이 이미 소유한 Vite 번들러 identity까지 끌어다 씀.** 리뷰어 표현: "the Tailwind v3 shape (`tailwind.config.{js,ts}` with a JS `theme.extend` object)... Tailwind v4's canonical configuration shape is a CSS-first `@theme { --color-*: ... }` block." 원리는 `docs/guidelines/adr-template.md` "Litmus test for durable Decision content" — 도구/통합 reference가 한 단계 위 durable claim 안에 박히면 그 도구의 다음 major 버전 / 교체 즉시 amendment 필요. → `4724f259`에서 단락을 ownership + 공유 토큰 소스 + consumer call site type-check 세 가지 property-level 진술로 재작성. `docs/adr/adr-0047-tailwind-as-editor-styling-default.md` Decision §2.

**지적 2 — Consequences가 `Tailwind's JIT mode emits only used utilities`라고 적었는데 v3 named-mode 라벨.** 리뷰어 표현: "v3 made the JIT engine the default and replaced the classic engine entirely; v4 folded it into the core engine and dropped the named-mode label." 원리는 #1과 동일 — 버전 핀된 용어가 durable Consequences에 들어가면 v4 독자가 "ADR이 stale하거나 v3에 pin됐다"고 읽음. → `4724f259`에서 `Tailwind emits only the utilities actually referenced by the editor's source`로 교체. v3 / v4 / 후속 버전 모두에서 holds.

**지적 3 — 두 finding 모두 `876b73dc`가 이미 PR 안에서 calibrate한 표준의 symmetric application.** PR 둘째 커밋에서 작성자가 `cn()` 도구 pin을 Decision에서 직접 제거했음 — 그 표준을 같은 ADR 안의 두 다른 단락에 일관되게 적용하지 않은 게 round 1의 진짜 defect class. → round 1 fix는 추가 작업이 아니라 **같은 표준의 일관 적용**.

> [!tip] 가장 중요한 배운 것 — PR 내부 자체 precedent를 끝까지 sweep해야 함
> 같은 PR 안에서 한 단락의 v-pin을 직접 제거했다면, 다른 단락에 남은 같은 shape의 v-pin은 **같은 자기-precedent의 미적용**이다. Pre-PR 셀프 리뷰에서 author가 한 번 calibrate한 표준은 같은 문서의 모든 단락에 sweep할 것 — round 1에서 같은 reviewer가 같은 표준으로 두 번 더 지적하는 건 셀프 sweep 갭의 신호.

> [!abstract] Rule
> ADR 안에서 도구·라이브러리·번들러 named-mode 라벨 / `tailwind.config.{js,ts}` 같은 파일-형태 / 특정 major 버전에서만 유효한 용어가 durable Decision / Consequences 본문에 들어가면 defect. property로 진술하라 — "JIT mode emits only used utilities" → "emits only the utilities actually referenced".
>
> #rule

> [!warning] PR 안에서 자기-precedent를 만든 commit (예: `876b73dc`) 는 round 1까지 가기 전 같은 문서의 나머지 단락을 sweep하는 기준이 되어야 한다
> 같은 PR 안의 commit이 표준을 calibrate하면, 그 표준은 그 commit 이후 PR 전체에 retroactive하게 적용된다. `876b73dc`(cn() pin 제거) 직후 같은 ADR의 v-pin 두 단락을 같은 셔틀에서 sweep했어야 함. **교훈:** pre-PR self-review 마지막 단계에 "이 PR 안에서 직접 만든 self-precedent를 같은 문서 전체에 적용했는가" 한 줄 체크 추가.

---

## 16:50 — STL-403 closed ([#316](https://github.com/CINEV/shotloom/pull/316))

회고 — UI feedback surface 문서 (toast / modal / banner / destructive modal / progress overlay). Round 1에서 placement Blocking + 6 nit, Round 2에서 APPROVED + 1 nit.

**지적 1 — Placement: `docs/guidelines/` 가 아니라 `docs/specs/` 였어야 함.** 리뷰어 인용: "AFDS v2 §5.4 explicitly lists 'UX flows' as in-scope for `docs/specs/`, and the sibling spec this file cross-references — `docs/specs/error-ux.md` — already lives there." `documentation-standard.md` §5.4가 "UX flows"를 specs 영역으로 명시하고 §5.10이 guidelines를 engineering process로 한정. 같은 PR 안에서 sibling `error-ux.md`를 참조하면서 그 옆이 아닌 다른 bucket에 둔 것이 구조적 defect — 동시에 별도 nit으로 지적된 "competing rubric 문제"의 직접 원인. → `8573a329`에서 `git mv` 로 `docs/specs/ui-feedback-surfaces.md` 이동, MAP.md + 두 README + PR title (`docs(guidelines):` → `docs(specs):`) 전부 동기화.

**지적 2 — 같은 PR 안의 도구/형태 pin 3건 (status, table 모호, identifier backtick).** ADR-0047을 status에 pin한 평문 → status-neutral linked reference. Selection 표의 `Modal dialog` vs `Destructive modal` Use-when 행이 같은 케이스(예: delete character)에 둘 다 해당 → "Destructive modal is a subtype of modal dialog" 한 줄 + `Modal dialog` 행을 non-destructive로 한정. Scope에서 plain text `global modal provider` vs Rules의 백틱 `ModalProvider` 불일치 → 양쪽 백틱. 셋 다 `adr-template.md` "rename-fragile prose" + identifier-style consistency 규칙에 매핑. → `8573a329`.

**지적 3 — Precedence rubric이 `error-ux.md` §2와 경합.** 리뷰어 인용: "Severity determines UI pattern... The mapping is fixed per failure mode" — error-ux.md의 기존 rubric. AFDS v2 §7.1 "no duplicate sources of truth". → Purpose 섹션에 명시: "For Alpha failure modes listed in `error-ux.md` §3, that spec's severity-driven mapping is canonical; for everything else, this spec is canonical." → `8573a329`. 이 한 줄이 두 spec sibling-level 경계를 깔끔하게 만듦.

**지적 4 — MAP.md가 toast primitive를 직접 가리키지 않음.** AFDS v2 §2 "MAP.md is the canonical answer to 'where is X?'". 새 spec이 `ToastProvider` / `useToast()` / `apps/editor/src/components/toast/` 를 명명하지만 MAP.md는 one-click으로 source에 도달 불가. → editor cluster에 `- Where is the editor toast provider and useToast() hook? → ...` 줄 추가. spec row는 specs 클러스터로 재배치. → `8573a329`.

**지적 5 (Round 2) — 링크에 anchor fragment 누락.** `[error-ux.md §3](error-ux.md)` 는 §3을 claim하지만 파일 top으로 떨어짐. 같은 저장소 sibling spec (`camera-clip-creation.md:90`)이 real fragment 컨벤션 (`camera-preset.md#3-shot-type-preset-and-camera-preset`) 사용. 만약 `error-ux.md`가 numbering을 바꾸면 `§3` 라벨은 silently drift하면서 URL은 유효해 보임. → `0b9407b5`에서 `#3-failure-mode-reference-table` 추가.

**Rebase 컨플릭트** — main에 `character-thumbnail-capture-prep.md` (PR #314)가 specs 클러스터에 land한 사이에 이 PR의 `ui-feedback-surfaces.md` 추가가 같은 행 영역과 충돌. 두 항목 모두 보존하는 형태로 resolve. force-with-lease push.

> [!tip] 가장 중요한 배운 것 — Plan의 placement 결정은 in-repo standard 적용 대상이다
> 이전 plan review에서 "이건 가이드라인일 뿐이지 법은 아님"이라 한 framing은 *문서 내용*에만 적용됨. **문서가 사는 디렉토리**는 AFDS v2 같은 in-repo standard가 binding하며, 어겼을 때 round 1 Blocking으로 잡힘. Plan에서 file path를 정할 때 `documentation-standard.md` §5 series를 한 번 grep하면 round 1 placement 충돌이 제거됨.

> [!abstract] Rule
> Plan에 새 doc path를 적기 전, `documentation-standard.md` §5의 destination matrix를 1회 grep하라. UX flows / product behavior → `docs/specs/`; engineering process → `docs/guidelines/`; system shape → `docs/arch/`; decisions → `docs/adr/`. Sibling spec이 이미 존재하는 경우 그 옆이 default destination.
>
> #rule

> [!warning] APPROVED + non-blocking nit 처리는 별도 사이클이 아니라 같은 PR cycle의 close-out
> Round 2의 P3 nit (anchor fragment)을 round 3로 따로 다루면 PR에 reply-only 라운드가 추가됨. `shotloom-respond-pr` skill의 APPROVED-state branch가 "y 옵션 = reply + thread resolve + 재리뷰 요청 생략"으로 정확히 이 경우를 처리. **교훈:** APPROVED + optional nit 패턴이 나오면 cycle close를 author 측에서 종결 — reviewer ping noise 생산하지 말 것.
