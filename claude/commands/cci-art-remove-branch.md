---
description: Clean up old art branch after new one takes over
argument-hint: "[old_branch] [new_branch]"
allowed-tools: Bash(git:*)
---

**Redirected to `/cci-manage-art-branch cleanup`.**

@~/.claude/skills/cci-manage-art-branch/SKILL.md

Execute the `cleanup` sub-command. Arguments: $ARGUMENTS
