- **Active `gh` account must be `tomlim2`.** Run `gh auth status` and confirm the `tomlim2` account is marked `Active: true`. The `deemotl` account's token is known to be invalid; using it will fail with `Invalid username or token` during `gh pr create` and `git push`.
- **Commit author must be `tomlim2 <deemo@vonvon.me>`.** Verify with `git log -1 --format="%an <%ae>"`. If wrong, `git config user.name tomlim2 && git config user.email deemo@vonvon.me && git commit --amend --reset-author --no-edit` before pushing.
- **Build gate — use `--exclude shotloom-desktop`:** `cargo check --workspace --exclude shotloom-desktop`. The `shotloom-desktop` binary has a pre-existing Tauri icon.png issue unrelated to our work; including it in workspace check produces a false-red.
- **Format + lint gates (CI-equivalent):** `cargo fmt --check` must be clean and `cargo clippy --workspace --exclude shotloom-desktop -- -D warnings` must produce zero warnings. The same `--exclude shotloom-desktop` that applies to `cargo check` applies to clippy — Shotloom CI runs both excluded. Any clippy output will block the PR.
- **Doc path validator:** `node scripts/validate-doc-paths.mjs` must report all path references verified. This runs pre-commit locally and in CI; broken markdown links (including relative paths in ADRs and specs) fail it.
- **CI rust coverage validator:** `node scripts/validate-ci-rust-coverage.mjs` must pass before push. Every workspace member crate must be either listed in a `cargo test -p <crate>` lane in `.github/workflows/code.yml` or in the validator's `EXCLUDED` map with a reason. Fails the `Code Gate` job on CI when a new crate is added without registration. **Common case:** new scaffold/extraction crates (e.g. STL-127's normalizer extractions) MUST be added to `rust-core-and-contracts` lane next to their sibling pipeline crates in the SAME PR that creates the crate — `cargo test -p` on a no-test crate exits 0, so the registration is harmless and future-proofs the lane the moment tests land. Auto-commit/push gate cycle (this same file) MUST run this validator after `cargo test` and before push; do not rely on remote CI to catch it.
- **Commit messages follow `docs/guidelines/commit-guideline.md`.** Conventional commits (`feat(scope): short summary`), lowercase type + scope, imperative mood, subject ≤ 80 chars, no trailing period, blank line before body, body wraps at 80 chars. Breaking changes require both `!` in subject and `BREAKING CHANGE:` in body.
- **Branch name `feat/<description>` — no Linear issue IDs in branch name.** Per CONTRIBUTING: use `feat/`, `fix/`, `chore/`, `hotfix/`, `release/v1.2.0` for version branches. Do NOT prefix with `stl-75-` or similar. Linear's auto-generated `deemo/stl-NN-…` is Linear's UI hint, not the canonical branch name.
- **Linear cross-reference in footer, not subject:** commit body may include `Related to STL-NN`. Do NOT use `Resolves STL-NN` in commits — closing linkage belongs in PR descriptions only.
- **PR title + description match Shotloom team pattern.** Sample the last 3–5 merged PRs in the Shotloom repo before drafting: `## Summary`, `## Test plan` (checkbox list). Draft status is appropriate when the PR is for ADR review or in-progress work.
- **`/claude-review` is a PR-comment trigger, NOT a Claude Code skill.** Posting the literal text `/claude-review` as a top-level PR comment fires the Claude review GitHub App on the CI side. Post **only after** all CI checks finish green (`gh pr checks <N> --watch`) and **only with explicit per-PR user approval** — never on a red PR (wasted review cycle), never on a draft unless promoted to ready, never inside `/shotloom-auto-pr` (auto-pr owns its review cadence). The author-side workflow lives in `/shotloom-make-pr` Step 10c, between the post-create devlog (10b) and the auto-pr offer (11).
- **PR body MUST include `## Related Issues` section** per `docs/guidelines/pr-guideline.md` §40. Allowed line shapes (one or more): `Resolves STL-NNN` (PR fully closes the issue — Linear auto-transitions Done), `Part of STL-NNN` (contributes but doesn't close), `Resolves STL-A, STL-B` (multi-issue comma-separated), or literal `No issue`. **Do NOT use** custom headings like `## Related`, `## References`, or footer-style `Related to STL-NNN` in place of this section — those are commit-body footers (per commit-guideline.md), not PR-body sections. Sub-issue parent linkage (e.g. STL-194 → STL-127) is implicit via Linear `parentId` and should NOT be duplicated in the PR body unless there is a real semantic reason. Supplementary cross-references (ADRs, external docs, source-of-truth links) go under a separate `## References` section, never inside `## Related Issues`.
- **If the PR touches an ADR**, update `docs/adr/README.md` to list the new ADR under the correct group (Accepted / Proposed / Superseded) and verify `validate-doc-paths.mjs` picks up the new file.
- **Pre-PR self-review must walk `~/.claude/standards/review-code-rust.md`.** This is the single source-of-truth Rust review checklist — patterns are added as new defect classes are caught on PRs, so the count and group letters drift. Do NOT memorize a count; re-read the file every PR. Groups currently span A doc↔code coherence, B classifier asymmetry, C silent fallback, D library hygiene, E build/platform, F cross-crate, G structural/repo-convention. Walk every pattern against every changed file and fix matches before opening the PR. Preferred workflow: `/shotloom-review-before-pr` (local, read-only) first, then `/shotloom-make-pr`. When dispatching `cci-codex-review-rust`, the skill already auto-includes `review-code-rust.md` for shotloom repos — you do not need to pass it via `--context-docs`. The old `shotloom-review-patterns.md` is now a redirect stub; do not load it directly.
- **Auto-commit + auto-push after gates pass (shotloom exception to `rules/git.md`).** Shotloom is primary work with tight deadlines — speed > per-step approval. Applies ONLY inside `shotloom` worktrees (including `.worktrees/*` and `.claude/worktrees/*`). Behavior: after fmt / clippy / check / tests / doc-paths all pass, draft the commit message, show it briefly for transparency, then commit and `git push` without waiting for user "OK". Gates are NEVER skipped. **PR operations still require per-PR approval** per `rules/git.md` — commit + push are auto-exempt, but `gh pr create`, `gh pr merge`, `gh pr close`, and posting any PR/review comment all remain explicit-approval. Other repos (CINEV, caol-ila, personal) remain under the strict approval flow.
- **`/shotloom-auto-pr` skill — additional blanket exemption (authorized 2026-04-21).** When the user explicitly invokes `/shotloom-auto-pr <N>` (or accepts the offer in `/shotloom-make-pr` Step 11), the skill's react cycle is exempt from per-PR-comment approval for the following PR-action set: posting inline review replies (`POST /pulls/<N>/comments/<id>/replies`), submitting a review-level summary reply for suppressed items via `POST /pulls/<N>/reviews` with `event=COMMENT`, re-requesting review (`POST /pulls/<N>/requested_reviewers`), refreshing the PR body (`gh pr edit <N> --body …`), and the auto-commit/auto-push already covered above. The exemption is **scoped to this skill alone** and **does NOT cover** `gh pr create`, `gh pr merge`, `gh pr close`, `gh pr reopen`, `gh pr ready`, `gh pr edit --base/--title/--draft/--label`, `gh pr update-branch` (rebase/merge of base into PR head), top-level PR comments via `/issues/<N>/comments`, `gh pr review --approve`/`--request-changes`, or thread resolution — those still require explicit per-action user approval even inside the auto-pr react cycle. Manual `/shotloom-respond-pr` is unaffected and keeps the per-comment batch approval gate; that skill auto-approves only `gh pr edit --body` (body refresh in Step 5-4) and never any other PR-state mutation.
- **Agent-specific operational docs live in `.agent/` at repo root**, not in `docs/`. Shotloom has a dedicated Codex agent ("돌쇠") for this repo; `docs/` is the authoritative project documentation that humans and all agents read, while `.agent/` is for informal operational guidance written for whichever agent is running (working rules, project reading order, pre/post-task checklists, repo-scoped playbooks). When writing a new convention: if it is project truth → `docs/`. If it is "how an agent should behave while working on this repo" → `.agent/`. Root `AGENTS.md` may reference `.agent/` files but should not duplicate their content. Recommended file set: `.agent/README.md` (index), `.agent/working-rules.md`, `.agent/project-guide.md`, `.agent/checklists.md`. Full path example: `/Users/deemooooooooo/Desktop/www/shotloom-github/.agent/`.
- Full reference: @~/.claude/standards/shotloom.md
