---
load: auto
---

- **Secrets** — use environment variables, NEVER store in this folder
- **Documentation language** — ALL skill (`SKILL.md`), rule (`rules/*.md`), and standard (`standards/*.md`) documents must be written in **English only**. No exceptions. Korean is allowed only in conversation with the user and in Obsidian vault notes.
